# app

> **API Route Handlers** — Handle HTTP requests, parse bodies, call service layer, return responses

Depends on: Application Service Layer, Infrastructure Layer

Exposes to: Frontend Components

## Files

- `globals.css`
- `layout.tsx`
- `page.tsx`

## Architecture Rules

- Each feature gets its own directory with route.ts
- Parameterized routes use Next.js bracket conventions
- App Router pages and layouts only in src/app
- Next.js App Router requires route.ts for API handlers
- Feature name + role suffix (Panel, Modal, Card, Frame)
- Action-first naming for exported functions
- Co-located test files with .test.ts or .integration.test.ts suffix

---
*Auto-generated by Archie.*